# cuddle. Backend Environment Variables

# Server
PORT=5000
NODE_ENV=development

# MongoDB (ciphertext only - no plaintext ever stored)
MONGODB_URI=mongodb://localhost:27017/cuddle

# JWT (for session tokens, NOT for encryption)
JWT_SECRET=your-jwt-secret-change-in-production

# CORS
CLIENT_URL=https://localhost:3000
